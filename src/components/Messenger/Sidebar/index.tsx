interface SideBarProps extends React.PropsWithChildren {

}
export const SideBar: React.FunctionComponent<SideBarProps> = ({ children }): JSX.Element => {
    return (
        <div>
            SideBar
        </div>
    )
}